import { SignupForm } from "@/components/auth/SignupForm";

export default function SignupPage() {
  return (
    <SignupForm
      title="Create your account"
      subtitle="After you create an account, you will be asked to sign in so we can issue a session token."
    />
  );
}
